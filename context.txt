7.	Content Revamp Topic Guide
Use this checklist while going through the app to confirm each section below still matches current functionality before rewriting the content above it. For every page, console, or role listed, capture a fresh screenshot, re-walk the described steps in the live app, and note anything new, removed, or changed.
General Overview
•	Confirm the one-line description of the app still matches its current positioning (a PWA for running and following ultimate frisbee leagues and tournaments, serving public viewers, players/fans, tournament staff, and offline field use).
•	Re-check the feature list against what the app currently offers.
•	Verify the app URL (https://stallcount.vercel.app/) is still correct.
•	Confirm PWA install/download behaviour and local-storage/offline claims still hold (offline queue, session auto-resume, cached queries).
•	Re-check the data usage and connectivity requirements described.
Public Pages
•	Home: reconfirm layout — hero live/next match (multi-card grid when 2+ live), next-scheduled strip, global stats metrics (teams/players/events), "Your notifications" (logged-in only: pinned teams + matches), live & upcoming list, featured broadcasts (matches with media links, "Next to stream" / "Latest replays"), latest results, and event timeline (current / upcoming / past). Note the mobile vs desktop item limits. Capture new screenshots.
•	Events: confirm the Active | Past | Upcoming event filter, the event details panel (name, dates, location, division), and the Current | Upcoming | Recent match list with media buttons and per-event workspace links.
•	Event (specific): confirm the custom per-event workspace layout, rules button, and roster button are still present, and that cards link to the workspace when one exists.
•	Matches: NOTE the detailed report is now login-gated — unauthenticated users see a "Match data hidden" banner with a sign-in prompt, and only the event/match selectors and header are public. When signed in, recheck each report component — match analytics (Match insight + Tempo insight tables), score progression graph (dual line, timeout/stoppage/halftime bands, possession timeline), team production (Holds, Clean holds, Total turnovers, Breaks, Break chances, plus Goals / Assists / Turnovers tables and a "Top connections" assist→scorer table), spirit score report (radar charts + notes), and the point-by-point log (Time, event symbol, Team, Assist → Scorer, score gap — including blocks ♻️ and callahans). Capture fresh screenshots of each.
•	Teams: confirm the paginated, searchable directory (20 per page, "Search by team or short code") and, on a team profile, the header metrics (W-L, win %, points for/against, +/-, spirit average) and the Games | Players & Stats | Spirit Scores tab layout.
•	Players: confirm the required event selector, optional division filter, name search, and sortable stats table (Player #, Name, Team, Goals, Assists, Blocks, Turnovers, Callahans, Total) with sort options.
•	Community: confirm the prominent Scrimmage console card (with feature tags: Live scoreboard, Offline-ready, PDF & CSV export, ABBA tracking, Spirit scores), the sticky jump-nav (Governance, Clubs, Rules, Season, Get started, Watch), the governance orgs (WFDF / AAFDF / SAFDA), and the regional club listings.
•	User: confirm the role-gated quick-access tool cards (Score keeper, Captain, Tournament director, Admin tools, Event access control), the global and event-scoped role assignments shown (role, event, dates, granted-by), and the role-library reference.
Initial Setup
•	Re-walk the signup flow: create account / log in (email-password or Google), notify an admin/TD for role access, then refresh the User page once assigned. NOTE there is no self-service "request a role" UI — roles are assigned directly by an admin or tournament director (global or event-scoped) and tracked with who granted them and when.
•	Confirm the Admin Tools list of back-end services is current. The Admin hub currently exposes: Score keeper (7v7 & 5v5), Scrimmage, Captain, Sys admin, Access control, Event access control, Signup management, Tournament director, Playoff structure, Media, Event setup, Custom scripts, Spirit scores. Note that Playoff structure is gated to admin / tournament_director and Custom scripts is admin-only.
Roles
•	Confirm the current role slug set is unchanged: admin (aka administrator / sys_admin), tournament_director, scorekeeper, field_assistant, captain, team_manager, media, user.
•	Viewer: confirm the read-only public page access list (Home, Events, Event overview, Event roster, Matches header only, Teams, Players, Community) — and note that detailed match reports now require login.
•	User: confirm the notification-access description is accurate (any logged-in user can manage push subscriptions).
•	Media: confirm media-URL add/modify/remove permissions are limited to the assigned event, and note the team-colour editing that also lives in Media Admin.
•	Score Keeper: confirm event-linked access and ability to edit/delete logged match events.
•	Field assistant: confirm match-status/roster edit permissions described are still accurate.
•	Captain: confirm roster add/edit rules, duplicate-check requirements (name, surname, date of birth, jersey), and spirit score / result confirmation.
•	Team manager: confirm whether this is still a placeholder role or has since been defined (it appears in the role list and shares spirit-score access with captains — verify its actual permissions).
•	Tournament director: re-verify the full permission list and each sub-page (Event setup, Event access control, Signup management, Tournament director view, Playoff structure, Custom scripts).
•	Admin: confirm the scope (assigning the initial TD to an event, plus system-wide user/role management via Sys admin) is unchanged.
Functions
•	Notifications: confirm the feature is LIVE (previously described as in development). Re-check the subscription targets (Match, Team, Player, Event, Division) and topics offered (match_start, score, match_final, turnover, timeout_start, stoppage_start, halftime_start), plus the browser push-permission prompt.
•	Scrimmage: confirm console behaviour — Light vs Dark teams, inline roster upload (team name on line 1, then jersey + name per line, assign players to A, B, or both), fully offline / local-only with no database writes, and PDF + CSV report download at the end. Confirm the localStorage session-persistence behaviour and its duration (the earlier "2 days" claim was not verified in code).
•	Score Keeper: re-walk the full workflow end to end — event & match selection, rules configuration, starting/pulling team, ABBA choice (mixed divisions), start match, scoring modal (scorer + assist, Callahan option), turnovers/blocks via the possession drag pad (Incompletion vs Block confirmation), inter-point timing (45s set offence, 60s signal ready), timeouts (0-75s timeout, extend/inter-point stages), halftime (manual or auto via point/time cap), discussion timer (15s involve captains, 45s declare contested), soft/hard cap banners, end match, and the spirit-scores redirect. Also confirm the offline pending-sync queue indicator and automatic session resume from localStorage after a refresh.
•	Score Keeper (5v5): confirm the stated differences from the full console (simpler rules display, no on-console ABBA UI, "End match" button with no spirit-scores redirect, success message on end) still hold.
•	Spirit Scores: confirm the match ID auto-fills from the score keeper redirect (?matchId= URL param) vs. manual event→match selection, the five WFDF sliders (Rules knowledge, Fouls & body contact, Fair-mindedness, Positive attitude, Communication) defaulting to 2, per-match draft persistence in localStorage, and the match status changing to "Completed" on submission.
•	Media: confirm the URL-linking behaviour for live streams/recordings (event → match → media URL, clear-media button, has_media flag) and the team-colour editing (accentColor, primaryColor, textOnPrimary, secondaryColor, textOnSecondary hex fields with preview).
•	Captain page: confirm rosters don't carry over between events, that players can be added/updated and removed from a roster, the duplicate-detection tiers (Exact name + DOB, Exact name, Likely duplicate, Similar name), and the Captain (C) / Spirit Captain (SC) role tags.
•	Event Access Control: confirm the main TD's ability to assign/remove other users' event permissions.
•	Tournament Director — Overview: confirm the summarised event/match progress view.
•	Tournament Director — Linked users: confirm the list of linked TDs, field assistants, captains, and media personnel and the role-management controls.
•	Tournament Director — Data Studio: confirm the table browser (select table, row limit 20/50/100/200, column filter, sort, insert/update/delete) and which tables are editable vs view-only (audit_log, match_events, match_logs are view-only).
•	Event Setup: re-walk the full wizard end to end — Event (name, type, dates, location) → Divisions (ruleset: format, division type, clock, game/half/inter-point rules, timeouts, mixed ratio/ABBA) → Pools → Teams & Matches (assign teams, generate seeded matches, set times) → Review → Confirm — and re-check the submission/success flow.
Streaming overlay
•	NOTE: no dedicated streaming-overlay / OBS module was found in the codebase during the last review — only team-colour JSON editing inside Media Admin and "featured broadcasts" (matches with media links) on the home page. Treat every item below as "confirm whether this feature still exists or was ever built" rather than assuming it works as written.
•	Confirm whether an overlay setup flow (match ID, overlay style, team colours) and any lock-to-Control-section behaviour exists.
•	Confirm the team colour JSON structure (accentColor, primaryColor, textOnPrimary, secondaryColor, textOnSecondary) still matches the database schema — this currently lives in Media Admin, not an overlay tool.
•	Confirm whether any Control-section banners exist (score banner, Break chance, Player stats, Match stats, Timeouts, Halftime + stoppage, Field calls, Team rosters).
•	Confirm whether a Rules summary, match log panel, Open overlay preview, and Copy URL button exist.
•	Confirm whether OBS setup steps (Browser source, pasted URL, 1980x1080 sizing) and the status-to-label mapping (Scheduled/Initialised/Live/Finished/Completed) are implemented anywhere.
